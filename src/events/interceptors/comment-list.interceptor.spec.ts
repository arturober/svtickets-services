import { CommentListInterceptor } from './comment-list.interceptor';

describe('CommentListInterceptor', () => {
  it('should be defined', () => {
    expect(new CommentListInterceptor()).toBeDefined();
  });
});
