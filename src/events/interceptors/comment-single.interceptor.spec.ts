import { CommentSingleInterceptor } from './comment-single.interceptor';

describe('CommentSingleInterceptor', () => {
  it('should be defined', () => {
    expect(new CommentSingleInterceptor()).toBeDefined();
  });
});
